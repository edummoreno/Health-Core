from fastapi import FastAPI
# from / import -> comandos de importação
# FastAPI -> Classe (um tipo de molde usado para criar objetos).
# Fastapi -> biblioteca que você instalou com pip install fastapi



#Cria um objeto da classe FastAPI

app = FastAPI(title="Health Core API")

# app -> variavel
# FastAPI(...) -> chamada do construtor, que cria a API
# titlte="Health Core API" -> argumento (um valor passado para a classe).

# Você está criando a sua API e guardando na variável app


# Isso é um decorator.

@app.get("/")

# @ → sintaxe de decorator
# app.get("/") → chama o método .get() do objeto app

# Você está criando uma rota GET no endereço:

# http://127.0.0.1:8000/


# def → comando que cria uma função
# read_root → nome da função
# () → não recebe parâmetros
def read_root():
    # Retorna um dicionário Python.
    return {"status": "ok", "message": "Health Core API Online"}

    # return → palavra-chave que devolve o valor da função
    # {...} → dicionário (estrutura de dados)
    # "status" e "message" → chaves
    # "ok" e "Health Core API Online" → valores

    # É isso que o usuário verá ao acessar a API.


# Comando que inicia o servidor Uvicorn (o motor da sua API).

uvicorn main:app --reload

# main:app
# main → nome do arquivo main.py
# app → nome da variável que contém a API
# “Rode a variável app dentro do arquivo main.py.”

--reload
# Isso é uma flag que diz: “Recarregue o servidor automaticamente quando eu alterar algum arquivo.”


# O que o Uvicorn está dizendo no terminal?

INFO: Uvicorn running on http://127.0.0.1:8000
INFO: Started reloader process
INFO: Application startup complete.

# ✔ Significado:

# O servidor está rodando.

# Ele está disponível em:
# 👉 http://127.0.0.1:8000

# Qualquer mudança que você salvar no VSCode vai recarregar a API automaticamente.